spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student
    username: postgres
    password: postgres
  server:
    servlet:
      context-path: /ex-beginner
  application:
    name: ex-beginner
